# my-dots-managed user overrides — sourced LAST by caelestia's hyprland.conf.
# The monitors-host.conf symlink is created by scripts/install.sh and points
# to hosts/<hostname>/monitors.conf.
source = ~/.config/caelestia/monitors-host.conf

# Add your own Hyprland directives below this line.
