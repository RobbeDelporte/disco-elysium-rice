# Yazi keymap — arrow key duplicates for navigation
# All other keybinds use Yazi defaults (vim hjkl)

[manager]
prepend_keymap = [
    # Arrow key navigation (mirrors hjkl defaults)
    { on = [ "<Up>" ],    run = "arrow -1",  desc = "Move cursor up" },
    { on = [ "<Down>" ],  run = "arrow 1",   desc = "Move cursor down" },
    { on = [ "<Left>" ],  run = "leave",     desc = "Go to parent directory" },
    { on = [ "<Right>" ], run = "enter",     desc = "Enter directory / open file" },
]
